a = 0
b = 0
c = 0
d = 0
e = 0
f = 0
g = 0
h = 0
i = 0
j = 0
k = 0
l = 0
m = 0
n = 0
o = 0
p = 0
q = 0
r = 0
s = 0
t = 0
u = 0
v = 0
w = 0
x = 0
y = 0
z = 0

para = int(input())

for loop in range(para):
    entrada = input()
    tamanho = len(entrada)
    saida = ""
    resp = 0
    for loop2 in range(tamanho):
        if (entrada[loop2] == 'a') or (entrada[loop2] == 'A'):
          a = a + 1
          if (a > resp):
            resp = a
            saida = "a"

        elif (entrada[loop2] == "b") or (entrada[loop2] == "B"):
          b = b + 1
          if (b > resp):
            resp = b
            saida = "b"

        elif (entrada[loop2] == "c") or (entrada[loop2] == "C"):
          c = c + 1
          if (c > resp):
            resp = c
            saida = "c"

        elif (entrada[loop2] == "d") or (entrada[loop2] == "D"):
          d = d + 1
          if (d > resp):
            resp = d
            saida = "d"

        elif (entrada[loop2] == "e") or (entrada[loop2] == "E"):
          e = e + 1
          if (e > resp):
            resp = e
            saida = "e"

        elif (entrada[loop2] == "f") or (entrada[loop2] == "F"):
          f = f + 1
          if (f > resp):
            resp = f
            saida = "f"

        elif (entrada[loop2] == "g") or (entrada[loop2] == "G"):
          g = g + 1
          if (g > resp):
            resp = g
            saida = "g"

        elif (entrada[loop2] == "h") or (entrada[loop2] == "H"):
          h = h + 1
          if (h > resp):
            resp = h
            saida = "h"

        elif (entrada[loop2] == "i") or (entrada[loop2] == "I"):
          i = i + 1
          if (i > resp):
            resp = i
            saida = "i"

        elif (entrada[loop2] == "j") or (entrada[loop2] == "J"):
          j = j + 1
          if (j > resp):
            resp = j
            saida = "j"

        elif (entrada[loop2] == "k") or (entrada[loop2] == "K"):
          k = k + 1
          if (k > resp):
            resp = k
            saida = "k"

        elif (entrada[loop2] == "l") or (entrada[loop2] == "L"):
          l = l + 1
          if (l > resp):
            resp = l
            saida = "l"

        elif (entrada[loop2] == "m") or (entrada[loop2] == "M"):
          m = m + 1
          if (m > resp):
            resp = m
            saida = "m"

        elif (entrada[loop2] == "n") or (entrada[loop2] == "N"):
          n = n + 1
          if (n > resp):
            resp = n
            saida = "n"

        elif (entrada[loop2] == "o") or (entrada[loop2] == "O"):
          o = o + 1
          if (o > resp):
            resp = o
            saida = "o"

        elif (entrada[loop2] == "p") or (entrada[loop2] == "P"):
          p = p + 1
          if (p > resp):
            resp = p
            saida = "p"

        elif (entrada[loop2] == "q") or (entrada[loop2] == "Q"):
          q = q + 1
          if (q > resp):
            resp = q
            saida = "q"

        elif (entrada[loop2] == "r") or (entrada[loop2] == "R"):
          r = r + 1
          if (r > resp):
            resp = r
            saida = "r"

        elif (entrada[loop2] == "s") or (entrada[loop2] == "S"):
          s = s + 1
          if (s > resp):
            resp = s
            saida = "s"

        elif (entrada[loop2] == "t") or (entrada[loop2] == "T"):
          t = t + 1
          if (t > resp):
            resp = t
            saida = "t"

        elif (entrada[loop2] == "u") or (entrada[loop2] == "U"):
          u = u + 1
          if (u > resp):
            resp = u
            saida = "u"

        elif (entrada[loop2] == "v") or (entrada[loop2] == "V"):
          v = v + 1
          if (v > resp):
            resp = v
            saida = "v"

        elif (entrada[loop2] == "w") or (entrada[loop2] == "W"):
          w = w + 1
          if (w > resp):
            resp = w
            saida = "w"

        elif (entrada[loop2] == "x") or (entrada[loop2] == "X"):
          x = x + 1
          if (x > resp):
            resp = x
            saida = "x"

        elif (entrada[loop2] == "y") or (entrada[loop2] == "Y"):
          y = y + 1
          if (y > resp):
            resp = y
            saida = "y"

        elif (entrada[loop2] == "z") or (entrada[loop2] == "Z"):
          z = z + 1
          if (z > resp):
            resp = z
            saida = "z"


    if (resp == a):
     print ("a" , end = "")
    if (resp == b):
     print ("b" , end = "")
    if (resp == c):
     print ("c" , end = "")
    if (resp == d):
     print ("d" , end = "")
    if (resp == e):
     print ("e" , end = "")
    if (resp == f):
     print ("f" , end = "")
    if (resp == g):
     print ("g" , end = "")
    if (resp == h):
     print ("h" , end = "")
    if (resp == i):
     print ("i" , end = "")
    if (resp == j):
     print ("j" , end = "")
    if (resp == k):
     print ("k" , end = "")
    if (resp == l):
     print ("l" , end = "")
    if (resp == m):
     print ("m" , end = "")
    if (resp == n):
     print ("n" , end = "")
    if (resp == o):
     print ("o" , end = "")
    if (resp == p):
     print ("p" , end = "")
    if (resp == q):
     print ("q" , end = "")
    if (resp == r):
     print ("r" , end = "")
    if (resp == s):
     print ("s" , end = "")
    if (resp == t):
     print ("t" , end = "")
    if (resp == u):
     print ("u" , end = "")
    if (resp == v):
     print ("v" , end = "")
    if (resp == w):
     print ("w" , end = "")
    if (resp == x):
     print ("x" , end = "")
    if (resp == y):
     print ("y" , end = "")
    if (resp == z):
     print ("z" , end = "")
    print ()
